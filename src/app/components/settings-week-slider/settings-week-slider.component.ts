import {Component, Input, OnInit} from '@angular/core';
import {LabelType, Options} from "@angular-slider/ngx-slider";
import {FormControl} from "@angular/forms";

@Component({
  selector: 'settings-week-slider',
  templateUrl: 'settings-week-slider.component.html'
})

export class SettingsWeekSliderComponent implements OnInit {



  constructor() {
  }

  ngOnInit() {
  }
}
