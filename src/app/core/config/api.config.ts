import { environment } from 'environments/environment';

export const API: string = environment.api;
export const NORMAL: string = environment.normal;
export const GOOGLE_KEY: string = environment.GOOGLE_KEY;
